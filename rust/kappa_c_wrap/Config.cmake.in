@PACKAGE_INIT@

include("${CMAKE_CURRENT_LIST_DIR}/kappa_c_wrapTargets.cmake")

set(kappa++_DIR "@kappa++_DIR@")
if(kappa++_DIR)
    include(CMakeFindDependencyMacro)
    find_dependency(kappa++ REQUIRED HINTS ${kappa++_DIR})
endif()

check_required_components(kappa_c_wrap)
